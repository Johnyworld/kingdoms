# Feature: Selection & Movement (선택과 이동)

> 스크립트: `scenes/game/game.gd`, `scenes/game/range_overlay.gd`

주인공 [부대](../entities/Party.md)를 선택하면 이동/공격 범위가 표시되고, 범위 안의 칸을 클릭해 이동한다.
움직이는 대상은 개별 [Human](../entities/Human.md)이 아니라 **부대(Party)**다.

## 좌클릭 처리 (`_handle_click`)

우선순위 순서:

1. **건물(캠프) 클릭이 최우선** — 선택 중이면 해제하고 [부대 정보 패널](party-info.md)을 닫은 뒤, [캠프 메뉴](camp-menu.md)를 연다.
2. **부대 칸 클릭** — 항상 [부대 정보 패널](party-info.md)을 연다(이동 완료 부대 포함). 아직 선택 전이고 **이동 가능(`can_move()`)** 하면 함께 선택(`_select`)해 이동 범위도 표시한다. **이번 턴에 이미 이동한 부대는 선택되지 않으므로 이동 범위는 표시되지 않는다**(정보 패널만).
3. **그 외 칸 클릭**:
   - 선택 상태에서 이동 범위(거리 1 ~ 이동력) 칸이면 그 칸으로 이동 후 안개 갱신. 이동하면 `party.mark_moved()`로 **이번 턴 이동 완료** 처리(흐리게 표시, 재선택 불가).
   - 이후 선택을 해제하고 부대 정보 패널을 닫는다.

> 부대는 **한 턴에 1회만** 이동할 수 있다([Turn](turn.md)). 이동한 부대는 흐리게 표시되고, 턴 종료 시 `reset_turn()`으로 다시 이동 가능해진다.

## 범위 계산 (`_update_ranges` → `HexGrid.movement_ranges`)

BFS와 범위 분할 규칙은 `scenes/game/hex_grid.gd`의 `HexGrid` 헬퍼로 분리되어 있다(시야 계산과 공유·테스트 용이).

- 부대 위치에서 **BFS**로 도달 셀과 거리를 계산.
- **이동력은 부대 기준** — `party.movement()`(멤버 이동력의 **최소값**, 가장 느린 멤버). 멤버 구성이 바뀌면 자동 반영.
- **이동 범위**: 거리 1 ~ `movement` → 파랑 헥스.
- **공격 범위**: 거리 `movement + 1` (마지막 링) → 빨강 헥스. 이 링에서는 더 확장하지 않는다.
- 부대가 선 칸(거리 0)은 제외.

## 범위 오버레이 (`range_overlay.gd`)

> `extends Node2D`

- `setup(terrain)` — 지형 참조 설정.
- `show_ranges(move_cells, attack_cells)` — 표시할 셀을 갱신하고 다시 그린다.
- 색상: 이동 `Color(0.2, 0.5, 1.0, 0.35)` 파랑 / 공격 `Color(1.0, 0.25, 0.25, 0.35)` 빨강.
- 각 셀에 타일 크기에 맞춘 헥스 폴리곤을 채워 실제 타일과 겹치게 그린다.

## 선택 상태

- `_select()` — 선택 플래그 on, 부대 강조 링 표시, 범위 계산.
- `_deselect()` — 플래그 off, 강조 링 제거, 범위 표시 지움.

## 테스트 시나리오

`test/unit/test_hex_grid.gd` (헥스 내부 기준: 거리 r 링 = 6r 셀).

- [정상] 이동력 1 → 이동 = 거리1 링 6셀, 공격 = 거리2 링 12셀
- [정상] 이동력 2 → 이동 = 거리1+2 (18셀), 공격 = 거리3 링 18셀
- [경계] 시작칸(거리 0)은 이동·공격 범위 모두에서 제외
- [경계] `dist` 맵에는 시작칸(거리 0) 포함 (이동 판정용)
- [경계] 맵 모서리에서 BFS가 맵 밖(음수/초과)으로 나가지 않음
